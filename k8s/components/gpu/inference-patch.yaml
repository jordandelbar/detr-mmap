apiVersion: apps/v1
kind: Deployment
metadata:
  name: inference
spec:
  template:
    spec:
      runtimeClassName: nvidia
      containers:
        - name: inference
          resources:
            limits:
              nvidia.com/gpu: 1
