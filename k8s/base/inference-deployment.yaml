apiVersion: apps/v1
kind: Deployment
metadata:
  name: inference
  labels:
    app: detr-mmap
    component: inference
spec:
  replicas: 1
  selector:
    matchLabels:
      app: detr-mmap
      component: inference
  template:
    metadata:
      labels:
        app: detr-mmap
        component: inference
    spec:
      # Node selector for edge simulation (can be overridden in overlays)
      nodeSelector:
        node-role: edge

      # Share host IPC namespace for shared memory (/dev/shm)
      # This enables mmap/semaphore IPC between capture/inference/gateway pods
      # Appropriate for single-tenant edge deployment (Raspberry Pi, Jetson, etc.)
      hostIPC: true

      volumes:
        - name: shm-bridge
          hostPath:
            path: /dev/shm
            type: Directory
        - name: models
          hostPath:
            path: /models
            type: Directory

      containers:
        - name: inference
          image: detr-mmap-inference:latest
          imagePullPolicy: Never

          securityContext:
            runAsUser: 0

          volumeMounts:
            - name: shm-bridge
              mountPath: /dev/shm
            - name: models
              mountPath: /models
              readOnly: true

          resources:
            requests:
              memory: "512Mi"
              cpu: "1000m"
            limits:
              memory: "2Gi"
              cpu: "4000m"

          env:
            - name: RUST_LOG
              value: "info"
            - name: ENVIRONMENT
              value: "production"

      restartPolicy: Always
