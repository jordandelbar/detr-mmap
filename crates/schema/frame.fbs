include "trace_context.fbs";

namespace bridge.schema;

table Frame {
    camera_id: uint32;
    frame_number: uint64;
    timestamp_ns: uint64;

    width: uint32;
    height: uint32;
    channels: uint8;
    pixels: [ubyte];

    trace: TraceContext;
}
